# Structure assets

Home page molvis uses **inline XYZ** in `docs/index.md` (no runtime fetch).

`aspirin.sdf` is kept only as a source file if you need to regenerate the
inline block; it is not loaded by the site.
